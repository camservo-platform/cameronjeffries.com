global:
  domain: argo.fuckamazon.org

configs:
  params:
    # This looks bad, but it's just here because SSL termination is done at the ingress.
    server.insecure: true
  rbac:
    policy.csv: |
      policy.csv: |
        g, ArgoCDAdmins, role:admin
  cm:
    # -- Create the argocd-cm configmap for [declarative setup]
    create: true
    ## TODO: Update to use PKCE.
    oidc.config: |
      name: Keycloak
      issuer: https://keycloak.fuckamazon.org/realms/fuckamazon-org
      clientID: argocd
      enablePKCEAuthentication: true
      requestedScopes: ['openid', 'profile', 'email', 'groups']

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
      nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    extraTls:
      - hosts:
        - argo.fuckamazon.org
        # Based on the ingress controller used secret might be optional
        secretName: argo-fuckamazon-org-tls
