apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-test.fullname" . }}
  labels:
    {{- include "cert-manager-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cert-manager-test.selectorLabels" . | nindent 4 }}
